# stylustx
StylusTx is a gas abstraction layer for Arbitrum that enables developers to sponsor transaction fees for their users. The system consists of three main components: a Stylus smart contract (paymaster), a TypeScript SDK, and a relayer service.
